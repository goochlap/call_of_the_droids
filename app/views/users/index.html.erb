  <h1>Users droids</h1>
  
  <ul>
    <% @droids.each do |droid| %>
      <li><%= droid.name %>
        <%= link_to 'delete', droid_path(droid), method: :delete, data: { confirm: 'Are you sure?'} %>
      </li>
    <% end %>
  </ul>